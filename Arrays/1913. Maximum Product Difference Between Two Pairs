class Solution {
    public int maxProductDifference(int[] nums) {
        // take two max elements and two minimum element 
        // to get maximized difference
        Arrays.sort(nums);
        return (nums[nums.length-1] * nums[nums.length-2]) - (nums[0] * nums[1]);
    }
}

class Solution {
    public int addDigits(int num) {
        // any number that adds upto 9 is divisible by 9
        // else the others are the remainder by 9
        
        if(num == 0) return 0;
        else if(num % 9 == 0) return 9;
        else return num % 9;
        
    }
}

class Solution {
    public boolean isPowerOfThree(int n){
        int ans = 1;
        while(ans<=n/3){
            ans *= 3;
            
        }
        if(ans == n)
            return true;
        else
            return false;
     
    }
}

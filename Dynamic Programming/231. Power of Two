class Solution {
    public boolean isPowerOfTwo(int n) {
        int ans = 1;
        while(ans<=n/2){
            ans *= 2;
            
        }
        if(ans == n)
            return true;
        else
            return false;
        
    }
}
